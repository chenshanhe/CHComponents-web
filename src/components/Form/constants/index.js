const TYPES = [
    "input","select"
]
const TYPES_LAYOUT = {
    input:"el-input",
    select:"ChSelect"
}
const TYPES_DEFAULT_VALUE ={
    input:"",
    select:null
}

const LABEL_POSITIONS = ["left","right","top"]

const SIZES = ["","medium","small","mini"]

export {
    TYPES,
    TYPES_LAYOUT,
    TYPES_DEFAULT_VALUE,
    LABEL_POSITIONS,
    SIZES
}